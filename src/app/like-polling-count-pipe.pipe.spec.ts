import { LikePollingCountPipePipe } from './like-polling-count-pipe.pipe';

describe('LikePollingCountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LikePollingCountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
