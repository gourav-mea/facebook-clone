server:
  port: 1000
spring:
  application:
    name: FriendProfileMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/infy_facebook_friendProfile
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update